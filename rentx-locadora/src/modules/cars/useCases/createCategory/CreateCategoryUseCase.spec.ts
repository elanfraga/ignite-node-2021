describe('Criar categoria', () => {
  it('Espero que 2 + 2 = 4', () => {
    const soma = 2 + 2;
    const resutado = 4;

    expect(soma).toBe(resutado);
  });

  it('Espero que 2 + 2 não seja 5', () => {
    const soma = 2 + 2;
    const resutado = 5;

    expect(soma).not.toBe(resutado);
  });
});
